name: profile-validation

on:
  workflow_dispatch:
  push:
    paths:
      - 'signals.py'
      - 'entry_engine.py'
      - 'target_quality.py'
      - 'live_replay.py'
      - 'backtest.py'
      - 'validation.py'
      - 'config.json'
      - 'tests/test_profile_validation.py'
      - 'tests/test_live_replay.py'
      - '.github/workflows/profile-validation.yml'

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Profile-aware historical validation
        run: python validation.py
      - name: Upload validation report
        uses: actions/upload-artifact@v4
        with:
          name: profile-validation
          path: state/profile_validation.json
          if-no-files-found: error
